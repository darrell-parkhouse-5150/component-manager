const fs = require('fs');
const path = require('path');
const { prompt } = require('inquire');

//! ask ai to finish these methods
const templates = {
    react: (name, desc) => {
        return ``
    }
}